# 2026-09-07

## 小程序前端开发任务文档 V1.3 编制（docs/v2/新泥报修系统 - 小程序前端开发任务文档 V1.3.md）
- 依据甲方对页面结构重组的需求另起新文档（V1.2 保留于 docs/v2/）。
- 核心变更：
  - TabBar「工单」→「服务」，新增服务主页 /pages/service/index（问候区 + 我的服务/工作服务分组入口）。
  - 入口矩阵（甲方确认）：业务员默认显示处理工单+企业管理，兼任审核员加显审核工单，兼任单位成员加显我的工单；超管四项全显。
  - 状态分组口径：待审核=reported；处理中=pending_accept+processing；结束=completed+cancelled（审核页/处理页同口径）。
  - 审核工单页：企业筛选 Header（单个/全部）+ 状态 Tab（待审核带计数）+ 新卡片样式；卡片不放操作按钮，动作收敛到详情页。
  - 处理工单恢复独立页面 /pages/admin/order/list，维修员卡片样式（图片行、企业+📍位置、大类·属性、联系人·脱敏电话）。
  - 新建工单页三段式：①发生了什么（照片+描述+常见问题**追加**填充，不替换）；②在哪里（报修单位/位置/联系人/联系电话）；③帮我们分类（设备类型=大类、问题类别=属性同栏左右分 + 紧急程度原位）。
  - 我的页移除工作入口卡，收敛到服务主页。
- 遗留待后端确认（文档第十一章）：contact 单字段需拆分联系人/联系电话（现库 varchar(40) 合一字段）、审核员"全部企业"聚合、通用常见问题接口（本期留空）、repairer_id=me。

## V1.3 开发落地（uni-app-frontend + backend）
- 新增：`pages/service/index.vue`（服务主页 Tab1 默认）、`pages/admin/order/list.vue`（处理工单）、`components/order-card`（审核/维修两种卡片）、`components/order-filter-bar`（企业筛选 Header + 状态分组 Tab）。
- 改造：`pages/order/list.vue` 由 Tab 页改为普通页「我的工单」（去掉角色切换/待办卡/动态标题）；`pages/review/index.vue` 重构为审核工单（企业筛选 + 待审核计数 + 新卡片，动作收敛到详情）；`pages/order/edit.vue` 三段式表单（常见问题改为**追加**描述；联系人/电话分栏，过渡期合并为 contact 提交）；`pages/profile/index.vue` 移除工作入口卡 + 加协议入口；登录/注册落地页改 switchTab 到服务主页；`pages.json` 更新。
- utils：`format.ts` 新增 relativeTime/splitContact/contactDisplay/descFirstLine/descSecondLine/REVIEW_TABS/REPAIR_TABS；`auth.ts` 新增 serviceEntryVisibility()/memberEnterprises()。
- 后端（为卡片渲染做的最小增量）：`AdminOrderItem` 增加 room/contact/images；`OrderImageRepository` 新增 `ListActiveByOrders`；`ListOrders` 组装故障图。go build/vet/test 全通过；实测 `/admin/orders?status=a,b` 支持逗号多值（文档第十一章 #2 已标记确认）。
- 实测账号提示：本地库「管理员」密码非 admin123（AGENTS.md 测试密码失效）；验收时可用 service.NewTokenService 直接签发 JWT 调接口。
- 前端校验方式：仓库无 vue-tsc，改用 `npx tsc --noEmit` + @vue/compiler-sfc 脚本解析 .vue 校验语法。

## 移除服务主页「审核工单」入口红点角标
- 甲方要求去掉服务主页审核工单入口的待审核数红点角标。
- 改动 `pages/service/index.vue`：删除入口处 `<text class="entry-badge">`、data 中 `reviewTodo`、方法 `loadReviewTodo()`、未登录分支的清零、`.entry-badge` 样式，以及因此不再使用的 `http`/`normalizePage` 导入。
- 保留：审核/处理页状态 Tab 内的「待审核 N」计数（属甲方原设计稿 `[待审核 3]`，非红点角标）。
- 后续甲方要求"都一并去了"：连同状态 Tab 计数徽标一并移除。改 `components/order-filter-bar/order-filter-bar.vue`（删模板 badge、`counts` prop、`countOf` 方法、`.fb-badge` 样式）与 `pages/review/index.vue`（删 `:counts`、`counts` data、`loadPendingCount` 方法及 loadList 内调用）。处理工单页未用计数，无改动。校验通过。
