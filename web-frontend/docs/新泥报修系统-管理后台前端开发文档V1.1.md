# 新泥报修系统 - 管理后台前端开发文档 V1.1

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 管理后台前端开发文档 |
| 版本 | V1.1 |
| 编制日期 | 2026年8月5日 |
| 技术栈 | Vue 3 + Vite + Element Plus + Pinia |
| 后端 API | https://api.xin-ni.com/api/v1 |

> **V1.1 更新说明**（对应后端接口文档 v1.0 更新）：
> 1. 工单列表 / 详情新增企业字段（`enterprise_id` / `enterprise_name`），金额等对账信息仅在详情展示
> 2. 5.6 完工接口新增必填对账字段：`quantity`、`unit_price`、`repair_content`、`metadata`（元数据以固定字段表单呈现）
> 3. 新增 5.6.1 修改对账信息接口（`POST /admin/orders/{id}/finance`），已完成工单可编辑对账信息
> 4. 新增 5.14 导出工单记录接口（`GET /admin/orders/export`），工单列表新增导出功能
> 5. 工单详情对账信息区块新增 `metadata` 元数据展示（5 个固定子字段：维修结果、维修方式、保修期、维修时长、额外备注），详见 6.2 节
> 6. 工单列表搜索栏重写：企业（下拉）/ 工单号 / 项目名称三项独立搜索 + 紧急程度 + 时间范围；表格各数据列支持点击表头升/降序（服务端排序），详见 6.1 节
> 7. 新增 5.15 维修员列表接口（`GET /admin/repairers`）；导出弹窗"业务员汇总"模式维修员改为下拉选择；业务员汇总 Excel 按企业分组 + 小计，详见 6.6 节

---

## 第一章 项目初始化

### 1.1 创建项目

```bash
pnpm create vite web-frontend --template vue-ts
cd web-frontend
pnpm install
pnpm add element-plus pinia vue-router axios
```

### 1.2 目录结构

```text
frontend-admin/
├── src/
│   ├── api/                    # API 请求
│   │   ├── client.ts           # axios 封装
│   │   ├── auth.ts             # 认证接口
│   │   ├── admin.ts            # 管理后台接口
│   │   └── enterprise.ts       # 企业管理接口
│   ├── stores/                 # Pinia
│   │   ├── user.ts             # 用户状态
│   │   └── enterprise.ts       # 企业状态
│   ├── views/
│   │   ├── login/
│   │   │   └── index.vue       # 登录页
│   │   ├── enterprises/
│   │   │   ├── list.vue        # 企业列表
│   │   │   └── detail.vue      # 企业详情
│   │   └── orders/
│   │       ├── list.vue        # 工单列表
│   │       └── detail.vue      # 工单处理
│   ├── layouts/
│   │   └── MainLayout.vue      # 主布局（侧边栏+顶栏）
│   ├── router/
│   │   └── index.ts            # 路由配置
│   ├── utils/
│   │   └── request.ts          # 请求封装
│   ├── types/
│   │   └── index.ts            # 类型定义
│   └── main.ts
└── package.json
```

---

## 第二章 路由配置

### 2.1 路由定义

```typescript
// router/index.ts
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { requiresAuth: false }
  },
  {
    path: '/',
    component: () => import('@/layouts/MainLayout.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: 'enterprises',
        name: 'EnterpriseList',
        component: () => import('@/views/enterprises/list.vue'),
        meta: { title: '企业管理', permission: 'platform_admin' }
      },
      {
        path: 'enterprises/:id',
        name: 'EnterpriseDetail',
        component: () => import('@/views/enterprises/detail.vue'),
        meta: { title: '企业详情', permission: 'platform_admin' }
      },
      {
        path: 'orders',
        name: 'OrderList',
        component: () => import('@/views/orders/list.vue'),
        meta: { title: '工单管理', permission: 'admin' }
      },
      {
        path: 'orders/:id',
        name: 'OrderDetail',
        component: () => import('@/views/orders/detail.vue'),
        meta: { title: '工单处理', permission: 'admin' }
      }
    ]
  }
]
```

### 2.2 路由守卫

```typescript
// router/index.ts
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  const userInfo = JSON.parse(localStorage.getItem('userInfo') || '{}')

  if (to.meta.requiresAuth) {
    if (!token) {
      next('/login')
      return
    }

    // 检查权限
    const required = to.meta.permission as string
    if (required === 'platform_admin' && userInfo.role !== 3) {
      next('/orders') // 平台管理员专属，跳转到工单管理
      return
    }
    if (required === 'admin' && userInfo.role < 2) {
      next('/login')
      return
    }
  }

  next()
})
```

---

## 第三章 API 封装

### 3.1 请求客户端

```typescript
// api/client.ts
import axios from 'axios'
import { ElMessage } from 'element-plus'

const client = axios.create({
  baseURL: 'https://api.xin-ni.com/api/v1',
  timeout: 10000
})

client.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

client.interceptors.response.use(
  response => {
    const data = response.data
    if (data.code !== 0) {
      // 业务错误
      ElMessage.error(data.message || '操作失败')
      return Promise.reject(data)
    }
    return data
  },
  error => {
    if (error.response?.status === 401) {
      // Token 过期
      localStorage.removeItem('token')
      window.location.href = '/login'
    } else {
      ElMessage.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default client
```

### 3.2 认证接口

```typescript
// api/auth.ts
import client from './client'

export interface LoginResponse {
  access_token: string
  expires_in: number
  user: {
    id: string
    nickname: string
    avatar_url: string
    phone: string
    role: number
    enterprises: Array<{
      enterprise_id: string
      enterprise_name: string
      role: string
      status: string
    }>
  }
}

export const authAPI = {
  // 微信回调登录（小程序用）
  login: (code: string) =>
    client.post<LoginResponse>('/auth/login', { code }),

  // 管理后台密码登录
  adminLogin: (data: { nickname: string; password: string }) =>
    client.post<LoginResponse>('/auth/admin-login', data),

  // 获取当前用户信息
  getMe: () =>
    client.get('/auth/me')
}
```

### 3.3 管理员接口

```typescript
// api/admin.ts
import client from './client'

export interface Order {
  id: string
  order_no: string
  reporter: {
    id: string
    nickname: string
    avatar_url: string
  }
  enterprise_id: string          // V1.1 新增：企业 ID
  enterprise_name: string        // V1.1 新增：企业名称
  project_name: string
  description: string
  urgency: 'normal' | 'urgent' | 'very_urgent'
  urgency_label: string
  status: 'reported' | 'reviewed' | 'processing' | 'completed' | 'cancelled'
  status_label: string
  image_count: number
  submitted_at: string
  created_at: string
}

/** V1.1 新增：维修附加元数据（完工/对账使用） */
export interface OrderMetadata {
  repair_result?: string      // 维修结果，如：完全修复
  repair_method?: string      // 维修方式，如：上门维修
  warranty_period?: string    // 保修期，如：3个月
  extra_remark?: string       // 额外备注
  repair_duration?: number    // 维修时长（分钟）
}

/** V1.1 新增：对账信息（详情返回 / 完工提交 / 修改对账共用） */
export interface FinanceInfo {
  quantity: number          // 数量
  unit_price: number        // 单价
  amount?: number           // 金额（后端 GENERATED 列自动计算，只读展示）
  repair_content: string    // 具体维修操作内容
  metadata: OrderMetadata
}

export interface AvailableAction {
  action: 'review' | 'accept' | 'complete' | 'reject' | 'update_finance'
  label: string
  to_status: string
  require_reason?: boolean
  reason_min_length?: number
  require_confirm?: boolean
  confirm_message?: string
}

// 工单详情 = 列表字段 + 对账信息 + 其他详情字段
export type OrderDetail = Order &
  FinanceInfo & {
    category: string
    category_label: string
    property: string
    property_label: string
    room: string
    contact: string
    reject_reason: string | null
    images: Array<{ id: string; url: string; sort_order: number; file_size: number }>
    receipts: Array<{ id: string; url: string; sort_order: number; file_size: number }>
    timeline: Array<{
      id: string
      action: string
      action_label: string
      operator_name: string
      from_status: string | null
      to_status: string | null
      remark: string | null
      created_at: string
    }>
    available_actions: AvailableAction[]
  }

export const adminAPI = {
  // 5.1 工单列表
  getOrders: (params: {
    page?: number
    page_size?: number
    status?: string
    urgency?: string
    keyword?: string
    enterprise_id?: string // V1.1：企业精确筛选（下拉选择）
    order_no?: string // V1.1：工单号模糊搜索（独立）
    project_name?: string // V1.1：项目名称模糊搜索（独立）
    date_from?: string
    date_to?: string
    reporter_id?: string
    sort_by?: string
    sort_order?: string
  }) => client.get('/admin/orders', { params }),

  // 5.2 工单详情
  getOrderDetail: (orderId: string) =>
    client.get<OrderDetail>(`/admin/orders/${orderId}`),

  // 5.3 查阅
  reviewOrder: (orderId: string, remark?: string) =>
    client.post(`/admin/orders/${orderId}/review`, { remark }),

  // 5.4 接单
  acceptOrder: (orderId: string, remark?: string) =>
    client.post(`/admin/orders/${orderId}/accept`, { remark }),

  // 5.5 退回
  rejectOrder: (orderId: string, reason: string) =>
    client.post(`/admin/orders/${orderId}/reject`, { reason }),

  // 5.6 完工（V1.1：新增必填对账字段）
  completeOrder: (orderId: string, data: {
    remark: string
    receipts: string[]
    quantity: number
    unit_price: number
    repair_content: string
    metadata: OrderMetadata
  }) => client.post(`/admin/orders/${orderId}/complete`, data),

  // V1.1 新增 5.6.1 修改对账信息（仅 completed 状态，字段至少传一个）
  updateFinance: (orderId: string, data: {
    quantity?: number
    unit_price?: number
    repair_content?: string
    metadata?: OrderMetadata
  }) => client.post(`/admin/orders/${orderId}/finance`, data),

  // V1.1 新增 5.14 导出工单记录（返回 Excel 文件流）
  exportOrders: (params: {
    mode: 'enterprise' | 'repairer'
    enterprise_id?: string
    repairer_id?: string
    date_from: string
    date_to: string
    fields?: string[]
    status?: string
  }) => client.get('/admin/orders/export', { params, responseType: 'blob' }),

  // 5.15 维修员(业务员)列表（导出弹窗下拉使用）
  getRepairers: () => client.get('/admin/repairers'),

  // 5.7 上传收据
  uploadReceipt: (orderId: string, file: File) => {
    const formData = new FormData()
    formData.append('file', file)
    return client.post(`/admin/orders/${orderId}/receipts`, formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    })
  },

  // 5.8 企业列表
  getEnterprises: (params: { page?: number; page_size?: number; keyword?: string; status?: string }) =>
    client.get('/admin/enterprises', { params }),

  // 5.9 企业详情
  getEnterpriseDetail: (enterpriseId: string) =>
    client.get(`/admin/enterprises/${enterpriseId}`),

  // 5.10 成员列表
  getMembers: (enterpriseId: string, params: { page?: number; page_size?: number; status?: string; role?: string; keyword?: string }) =>
    client.get(`/admin/enterprises/${enterpriseId}/members`, { params }),

  // 刷新邀请码（参考 3.9）
  refreshInviteCode: (enterpriseId: string, validity: string) =>
    client.post(`/enterprises/${enterpriseId}/refresh/code`, { validity }),

  // 审核通过（参考 3.6）
  approveMembers: (enterpriseId: string, userIds: string[]) =>
    client.put(`/enterprises/${enterpriseId}/members/approve`, { user_ids: userIds }),

  // 拒绝申请（参考 3.7）
  rejectMembers: (enterpriseId: string, userIds: string[]) =>
    client.put(`/enterprises/${enterpriseId}/members/reject`, { user_ids: userIds }),

  // 移除成员（参考 3.8）
  removeMember: (enterpriseId: string, userId: string) =>
    client.delete(`/enterprises/${enterpriseId}/members/${userId}`)
}
```

---

## 第四章 登录页

### 4.1 页面概述

```text
┌──────────────────────────────────────────────────────┐
│                                                      │
│               [Logo]  新泥维修后台                   │
│                                                      │
│    ┌─────────────────────────────────────────┐       │
│    │  账号密码登录                          │       │  ← 昵称 + 密码
│    │  昵称: [________________]              │       │
│    │  密码: [________________]              │       │
│    │          [登 录]                       │       │
│    └─────────────────────────────────────────┘       │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 4.2 交互流程

1. 用户输入昵称和密码
2. 调用 POST /auth/admin-login，传入 { nickname, password }（仅平台管理员 role=1 可登录成功）
3. 登录成功后存储 token 和 userInfo
4. 判断 role：平台管理员（role=1）→ /enterprises；其他 → 提示无权限并登出

> **V1.1 更新**：登录方式改为昵称 + 密码，原"微信授权码 Code"输入已移除。

### 4.3 伪代码

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'
import { ElMessage } from 'element-plus'

const nickname = ref('')
const password = ref('')
const loading = ref(false)
const router = useRouter()
const userStore = useUserStore()

const handleLogin = async () => {
  if (!nickname.value.trim() || !password.value) {
    ElMessage.warning('请输入昵称和密码')
    return
  }
  loading.value = true
  try {
    const res = await authAPI.adminLogin({ nickname: nickname.value.trim(), password: password.value })
    userStore.setUser(res.data.user, res.data.access_token)
    // 仅平台管理员可登录
    if (!userStore.isPlatformAdmin) {
      ElMessage.error('该账号无管理权限')
      userStore.logout()
      return
    }
    ElMessage.success('登录成功')
    router.push('/enterprises')
  } finally {
    loading.value = false
  }
}
</script>
```

---

## 第五章 企业管理模块（仅平台管理员 role=3）

### 5.1 企业列表页

**路径：** /enterprises

```text
┌──────────────────────────────────────────────────────────────────────┐
│  企业管理                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐│
│  │  搜索企业： [________]  [搜索]              [创建企业]           ││
│  └───────────────────────────────────────────────────────────────────┘│
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────────┐│
│  │  企业名称      │ 成员数 │ 工单数 │ 待审核 │ 状态 │ 操作       ││
│  ├───────────────────────────────────────────────────────────────────┤│
│  │  新泥维修总店   │ 45    │ 128   │ 3     │ 正常 │ [查看]     ││
│  │  新泥分店A     │ 23    │ 67    │ 0     │ 正常 │ [查看]     ││
│  └───────────────────────────────────────────────────────────────────┘│
│                                                                      │
│  共 15 条  < 1 2 3 >                                                │
└──────────────────────────────────────────────────────────────────────┘
```

**API 调用：**

```typescript
// 页面加载
const fetchList = async () => {
  const res = await adminAPI.getEnterprises({
    page: currentPage.value,
    page_size: 20,
    keyword: keyword.value
  })
  list.value = res.data.list
  total.value = res.data.total
}
```

### 5.2 企业详情页

**路径：** /enterprises/:id

```text
┌──────────────────────────────────────────────────────────────────────┐
│  ← 返回列表    新泥维修总店                                         │
├──────────────────────────────────────────────────────────────────────┤
│  企业信息卡片                                                        │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │  企业名称：新泥维修总店    成员数：45人  工单数：128单        │ │
│  │  邀请码：A3K9M2  [刷新]  [下载二维码]  状态：正常             │ │
│  │  免审核：未开启   创建时间：2026-01-01                        │ │
│  │  [编辑]                                                        │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  成员列表（45人）                                    [全部][待审核] │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ 昵称   │ 角色   │ 状态   │ 工单数 │ 加入时间   │ 操作       │ │
│  ├────────────────────────────────────────────────────────────────┤ │
│  │ 张三   │ 管理员 │ 已通过 │ 5     │ 2026-07-20 │ [移除]     │ │
│  │ 李四   │ 成员   │ 待审核 │ 0     │ 2026-08-02 │ [通过][拒绝]│ │
│  └────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

**API 调用：**

```typescript
// 1. 获取企业详情
const enterprise = await adminAPI.getEnterpriseDetail(id)

// 2. 获取成员列表（支持 status 筛选）
const members = await adminAPI.getMembers(id, {
  status: filterStatus.value, // '' | 'pending' | 'approved' | 'removed'
  page: page.value,
  page_size: 20
})

// 3. 更新企业设置（编辑弹窗：企业名称 + 免审核开关，均必传）
await adminAPI.updateEnterprise(id, {
  name: '新泥维修总店',
  auto_approve: false // true=免审核，新成员加入无需管理员审核
})

// 4. 审核通过
await adminAPI.approveMembers(id, [userId])

// 5. 拒绝申请
await adminAPI.rejectMembers(id, [userId])

// 6. 移除成员
await adminAPI.removeMember(id, userId)

// 7. 刷新邀请码
const res = await adminAPI.refreshInviteCode(id, 'permanent')
```

**编辑弹窗：**

| 字段 | 控件 | 校验 | 说明 |
| --- | --- | --- | --- |
| 企业名称 | el-input | 2-50 字符，不可重名 | 提交 `name` |
| 免审核 | el-switch | 无 | 提交 `auto_approve`，开启后新成员扫码加入无需审核 |

**状态 Tab：**

| Tab | 参数 | 说明 |
| --- | --- | --- |
| 全部 | status=all | 所有成员 |
| 已通过 | status=approved | 已审核通过的成员 |
| 待审核 | status=pending | 等待审核的成员 |
| 已移除 | status=removed | 已移除的成员 |

---

## 第六章 工单管理模块

### 6.1 工单列表页

**路径：** /orders

```text
┌──────────────────────────────────────────────────────────────────────┐
│  工单管理                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐│
│  │ [全部] [待查阅] [已阅] [处理中] [已完成] [已取消]               ││
│  └───────────────────────────────────────────────────────────────────┘│
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────────┐│
│  │  企业：[全部▾]  工单号：[____]  项目名称：[____]                ││
│  │  紧急程度：[全部▾]  时间：[____]~[____]                          ││
│  │  [搜索] [重置]                                      [导出]     ││
│  └───────────────────────────────────────────────────────────────────┘│
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────────┐│
│  │ 工单号▲   │ 企业 │ 报修人 │ 项目名称  │ 紧急度 │ 状态   │ 操作 ││
│  ├───────────────────────────────────────────────────────────────────┤│
│  │ WO001    │ 总店 │ 张三   │ 财务科电脑│ 🔴紧急 │ 已上报 │[查看]││
│  │ WO002    │ 分店A│ 李四   │ 打印机卡纸│ 🟡普通 │ 已阅   │[查看]││
│  └───────────────────────────────────────────────────────────────────┘│
│                                                                      │
│  共 128 条  < 1 2 3 >                                              │
└──────────────────────────────────────────────────────────────────────┘
```

**状态 Tab：**

| Tab | status 参数 | 说明 |
| --- | --- | --- |
| 全部 | 不传 | 所有非草稿状态 |
| 待查阅 | reported | 已提交等待查阅 |
| 已阅 | reviewed | 已查阅等待接单 |
| 处理中 | processing | 正在维修 |
| 已完成 | completed | 已完工 |
| 已取消 | cancelled | 已取消 |

**搜索栏（V1.1：三个独立搜索）：**

| 搜索项 | 参数 | 交互 | 说明 |
| --- | --- | --- | --- |
| 企业 | `enterprise_id` | 下拉选择（filterable） | 从 `/admin/enterprises` 加载，精确筛选 |
| 工单号 | `order_no` | 输入框 | 工单号模糊搜索（独立于其他条件） |
| 项目名称 | `project_name` | 输入框 | 项目名称模糊搜索（独立于其他条件） |
| 紧急程度 | `urgency` | 下拉选择 | `normal` / `urgent` / `very_urgent` |
| 时间范围 | `date_from` / `date_to` | 日期区间 | 基于提交时间 |

> 三项搜索互不干扰、可组合使用；已移除原合并 `keyword` 输入框（后端保留 `keyword` 参数兼容）。

**API 调用：**

```typescript
const fetchOrders = async () => {
  const res = await adminAPI.getOrders({
    page: currentPage.value,
    page_size: 20,
    status: currentStatus.value, // 不传 = 全部
    enterprise_id: enterpriseFilter.value, // 企业（下拉选择）
    order_no: orderNoFilter.value, // 工单号（独立）
    project_name: projectNameFilter.value, // 项目名称（独立）
    urgency: urgencyFilter.value,
    date_from: dateFrom.value,
    date_to: dateTo.value,
    sort_by: sortBy.value, // 点击表头排序时设置
    sort_order: sortOrder.value
  })
  list.value = res.data.list
  total.value = res.data.total
}
```

**V1.1 新增说明：**
- 表格新增"企业"列，展示 `row.enterprise_name`（列表接口新增返回 `enterprise_id` / `enterprise_name`）
- 金额等对账信息（数量/单价/金额/维修内容）**不在列表展示**，仅详情页展示（见 6.2）
- 【导出】按钮打开导出弹窗（见 6.6 导出工单），调用 5.14 导出接口下载 Excel
- **列排序**：除"操作"列外，所有数据列（工单号/企业/报修人/项目名称/紧急度/状态/提交时间）均可点击表头排序（`sortable="custom"`），触发 `sort-change` 事件后以 `sort_by` / `sort_order` 参数请求后端服务端排序；点击顺序：升序 → 降序 → 取消
- **排序字段映射**：`order_no`→工单号、`enterprise_name`→企业、`reporter`→报修人昵称、`project_name`→项目名称、`urgency`→紧急度、`status`→状态、`submitted_at`→提交时间、`created_at`→创建时间

### 6.2 工单处理页

**路径：** /orders/:id

```text
┌──────────────────────────────────────────────────────────────────────┐
│  ← 返回列表    WO202607310001  财务科电脑维修                       │
│  报修人：张三 ｜ 紧急程度：🔴紧急 ｜ 状态：已上报                   │
├──────────────────────────────────────────────────────────────────────┤
│  报修信息                          │  操作区域                      │
│  ┌──────────────────────────────┐ │  ┌───────────────────────────┐ │
│  │ 报修企业：新泥维修总店      │ │  │  状态：已上报             │ │
│  │ 项目名称：财务科电脑维修     │ │  │                           │ │
│  │ 报修位置：三楼财务科301      │ │  │  [查阅]  [退回]          │ │
│  │ 联系电话：13800138000        │ │  │                           │ │
│  │ 报修描述：台式机无法开机...  │ │  └───────────────────────────┘ │
│  └──────────────────────────────┘ │                              │
│                                    │  退回弹窗（点击退回时弹出）   │
│  图片                              │  ┌───────────────────────────┐ │
│  [img1] [img2]                    │  │ 退回原因：               │ │
│                                    │  │ [________]              │ │
│  对账信息（仅 completed 展示）     │  │ 10-200字符              │ │
│  ┌──────────────────────────────┐ │  │  [取消]   [确认退回]    │ │
│  │ 数量:2  单价:150  金额:300  │ │  └───────────────────────────┘ │
│  │ 维修内容:更换台式机电源模块  │ │                              │
│  │ [修改对账信息]               │ │                              │
│  └──────────────────────────────┘ │                              │
│                                    │                              │
│  附加信息（仅 completed 展示）     │  │                              │
│  ┌──────────────────────────────┐ │                              │
│  │ 维修结果:完全修复            │ │                              │
│  │ 维修方式:上门维修            │ │                              │
│  │ 保修期:2个月                 │ │                              │
│  │ 维修时长:45分钟              │ │                              │
│  │ 额外备注:USB牌子             │ │                              │
│  └──────────────────────────────┘ │                              │
│                                    │                              │
│  时间轴                            │                              │
│  2026-08-02 14:30 张三 创建草稿   │                              │
│  2026-08-02 14:32 张三 提交报修   │                              │
│                                    │                              │
└──────────────────────────────────────────────────────────────────────┘
```

**状态与操作对应：**

| 当前状态 | 可用操作 | 按钮显示 |
| --- | --- | --- |
| reported | review、reject | 查阅、退回 |
| reviewed | accept、reject | 接单、退回 |
| processing | complete、reject | 完工、退回 |
| completed | update_finance（V1.1） | 修改对账信息 |
| cancelled | 无 | 仅查看 |

**V1.1 新增说明：**
- 报修信息新增"报修企业"项，展示 `enterprise_name`
- 对账信息区块仅在 `completed` 状态展示：数量（`quantity`）、单价（`unit_price`）、金额（`amount`，只读）、维修内容（`repair_content`）
- 附加信息区块（仅 `completed` 展示）：展示 `metadata` 元数据，子字段映射如下：

| metadata 子字段 | 展示标签 | 格式说明 |
| --- | --- | --- |
| `repair_result` | 维修结果 | 直接展示，空值显示 `-` |
| `repair_method` | 维修方式 | 直接展示，空值显示 `-` |
| `warranty_period` | 保修期 | 直接展示，空值显示 `-` |
| `repair_duration` | 维修时长 | 数字 + "分钟"，如 `45 分钟`；空值显示 `-` |
| `extra_remark` | 额外备注 | 直接展示，空值显示 `-` |
- `completed` 状态下操作区显示【修改对账信息】按钮（动作 `update_finance`），打开 6.5 修改对账信息弹窗；当详情接口 `available_actions` 中返回该动作时按 action 驱动渲染

### 6.3 各操作的交互

#### 6.3.1 查阅

```typescript
const handleReview = async () => {
  await adminAPI.reviewOrder(orderId, remark.value)
  ElMessage.success('已查阅')
  fetchDetail()
}
```

#### 6.3.2 接单

```typescript
const handleAccept = async () => {
  await adminAPI.acceptOrder(orderId, remark.value)
  ElMessage.success('已接单')
  fetchDetail()
}
```

#### 6.3.3 退回（弹窗）

```typescript
const handleReject = async () => {
  if (rejectReason.value.length < 10) {
    ElMessage.warning('退回原因至少10个字')
    return
  }
  await adminAPI.rejectOrder(orderId, rejectReason.value)
  ElMessage.success('已退回')
  fetchDetail()
}
```

#### 6.3.4 完工（弹窗）

```typescript
const handleComplete = async () => {
  // 1. 先上传收据图片
  const receiptUrls = []
  for (const file of receiptFiles.value) {
    const res = await adminAPI.uploadReceipt(orderId, file)
    receiptUrls.push(res.data.url)
  }

  // 2. 组装对账信息（V1.1 新增必填字段）
  const financeForm = {
    quantity: quantity.value,            // 数量（≥0）
    unit_price: unitPrice.value,         // 单价（≥0）
    repair_content: repairContent.value, // 维修操作内容（必填）
    metadata: {
      repair_result: repairResult.value,
      repair_method: repairMethod.value,
      warranty_period: warrantyPeriod.value,
      extra_remark: extraRemark.value,
      repair_duration: repairDuration.value // 分钟
    }
  }

  // 3. 提交完工
  await adminAPI.completeOrder(orderId, {
    remark: remark.value,
    receipts: receiptUrls,
    ...financeForm
  })
  ElMessage.success('完工成功')
  fetchDetail()
}
```

#### 6.3.5 修改对账信息（V1.1 新增）

```typescript
const handleUpdateFinance = async () => {
  // 以弹窗表单当前值提交，未修改的字段保持原值（后端字段可选，至少传一个）
  await adminAPI.updateFinance(orderId, {
    quantity: financeForm.quantity,
    unit_price: financeForm.unit_price,
    repair_content: financeForm.repair_content,
    metadata: financeForm.metadata
  })
  ElMessage.success('对账信息已更新')
  fetchDetail()
}
```

### 6.4 完工弹窗

```text
┌─────────────────────────────────────────────┐
│  完工                                        │
│                                             │
│  维修备注：                                  │
│  ┌─────────────────────────────────────────┐│
│  │ 已更换电源模块，恢复正常使用           ││
│  └─────────────────────────────────────────┘│
│  ≤200字符（必填）                           │
│                                             │
│  对账信息（V1.1 新增，全部必填）            │
│  数量：[ 1 ]      单价：[ 150.00 ]          │
│  维修操作内容：                              │
│  ┌─────────────────────────────────────────┐│
│  │ 更换台式机电源模块，长城600W           ││
│  └─────────────────────────────────────────┘│
│  ≤200字                                    │
│                                             │
│  维修附加信息（可选，默认空）               │
│  维修结果：[完全修复▾]  维修方式：[上门维修▾]│
│  保修期：[ 3个月 ]     维修时长(分钟)：[ 45 ]│
│  额外备注：[ 已测试正常 ]                   │
│                                             │
│  收据图片：                                  │
│  ┌─────────────────────────────────────────┐│
│  │ [ + 上传图片 ]                          ││
│  │  [img1] [img2]                         ││
│  └─────────────────────────────────────────┘│
│  最多 3 张（jpg/png/webp，单张≤5MB）        │
│                                             │
│  [取消]              [确认完工]             │
└─────────────────────────────────────────────┘
```

**V1.1 新增说明：**
- 新增"对账信息"区块：数量（`quantity`，≥0）、单价（`unit_price`，≥0）、维修操作内容（`repair_content`，必填 ≤200字），均为必填
- 新增"维修附加信息"区块（对应 `metadata`）：维修结果、维修方式（下拉），保修期、维修时长（分钟）、额外备注（输入），默认空、均可选；提交时整体组装为 `metadata` 对象
- 收据上传逻辑不变（先调 5.7 逐张上传，再随完工接口全量提交 `receipts`）
- 校验：数量/单价为数字且 ≥0，维修内容必填，备注 ≤200字

---

### 6.5 修改对账信息弹窗（V1.1 新增）

**入口：** 工单详情页 `completed` 状态 →【修改对账信息】（后端 `available_actions` 返回 `update_finance` 时显示）

**接口：** 5.6.1 `POST /admin/orders/{order_id}/finance`（字段全可选，至少传一个；`amount` 由后端自动重算）

```text
┌─────────────────────────────────────────────┐
│  修改对账信息                                 │
│  工单号：WO202608010001（只读）               │
│  数量：[ 2 ]         单价：[ 150.00 ]        │
│  金额：300.00（只读，自动计算展示）          │
│  维修操作内容：                              │
│  ┌─────────────────────────────────────────┐│
│  │ 更换台式机电源模块，长城600W（已升级） ││
│  └─────────────────────────────────────────┘│
│                                             │
│  维修附加信息                                │
│  维修结果：[完全修复▾]  维修方式：[上门维修▾]│
│  保修期：[ 6个月 ]    维修时长(分钟)：[ 60 ]│
│  额外备注：[ 客户追加了一个U盘 ]             │
│                                             │
│  [取消]              [保存修改]              │
└─────────────────────────────────────────────┘
```

**交互说明：**
- 打开时用详情接口返回的 `quantity` / `unit_price` / `repair_content` / `metadata` 回填表单
- 金额列只读展示 `amount`（前端可实时用 数量×单价 预览，最终以后端重算结果为准）
- 提交 `updateFinance`，成功后端写入时间轴（记录修改前后差异），前端刷新详情
- 校验：数量/单价 ≥0 数字，维修内容 ≤200字

---

### 6.6 导出工单弹窗（V1.1 新增）

**入口：** 工单列表页【导出】按钮

**接口：** 5.14 `GET /admin/orders/export`，返回 Excel 文件流

```text
┌─────────────────────────────────────────────┐
│  导出工单记录                                 │
│  导出模式：○ 企业对账单 ○ 业务员汇总         │
│                                             │
│  企业：[新泥维修总店▾]（mode=enterprise 必填）│
│  维修员：[李四▾]（mode=repairer 必填）       │
│  日期范围：[2026-07-01] ~ [2026-07-31]       │
│  （必填）                                    │
│  工单状态：已完成（默认）                    │
│                                             │
│  导出字段（默认全部勾选以下 7 项）：         │
│  ☑工单号 ☑日期 ☑内容 ☑数量 ☑单价 ☑金额 ☑备注│
│  □维修结果 □维修方式 □保修期 □维修时长      │
│  □报修人 □维修员 □位置 □联系方式 □客户名称  │
│                                             │
│  [取消]              [确认导出]              │
└─────────────────────────────────────────────┘
```

**交互说明：**
- 导出模式二选一（`mode`）：`enterprise`=企业对账单，`repairer`=业务员汇总
- 联动展示选择器：`enterprise` 显示企业下拉（来源 5.8 企业列表），`repairer` 显示维修员下拉（数据来源 5.15 `GET /admin/repairers`，仅平台管理员）
- 日期范围必填，转换为 `date_from` / `date_to`（YYYY-MM-DD）
- 字段勾选（`fields`）：默认勾选 `order_no, time, content, quantity, unit_price, amount, remark`，其余为可选列（见 5.14 可用字段表）
- 提交后 `responseType: 'blob'` 接收文件流，按响应头 `Content-Disposition` 的 filename（或默认"工单导出.xlsx"）触发浏览器下载
- 空数据时后端返回错误码 `4507`，前端提示"当前筛选条件下没有可导出的数据"

**V1.1 更新：**
- 业务员模式"维修员"改为下拉选择（数据来源 `GET /admin/repairers`，仅平台管理员）
- 业务员汇总导出 Excel 按企业分块（小组标题 + 小计行），底部全局合计

---

## 第七章 全局状态管理

### 7.1 userStore

```typescript
// stores/user.ts
export const useUserStore = defineStore('user', {
  state: () => ({
    token: '',
    userInfo: null as UserInfo | null
  }),
  actions: {
    setUser(user: UserInfo, token: string) {
      this.userInfo = user
      this.token = token
      localStorage.setItem('token', token)
      localStorage.setItem('userInfo', JSON.stringify(user))
    },
    logout() {
      this.userInfo = null
      this.token = ''
      localStorage.removeItem('token')
      localStorage.removeItem('userInfo')
    },
    getRole() {
      return this.userInfo?.role || 0
    }
  },
  getters: {
    isPlatformAdmin: (state) => state.userInfo?.role === 3,
    isEnterpriseAdmin: (state) => state.userInfo?.role === 2
  }
})
```

---

## 第八章 关键 UI 细节

### 8.1 状态标签颜色映射

| 状态 | Element Plus Tag 类型 | 颜色 |
| --- | --- | --- |
| reported | danger | 红色 |
| reviewed | warning | 橙色 |
| processing | primary | 蓝色 |
| completed | success | 绿色 |
| cancelled | info | 灰色 |

### 8.2 紧急程度标识

| 程度 | 颜色 | 图标 |
| --- | --- | --- |
| normal | 蓝色 | ● |
| urgent | 橙色 | ●● |
| very_urgent | 红色 | ●●● |

### 8.3 二维码生成

```typescript
import QRCode from 'qrcode'

const generateQR = async (code: string) => {
  const url = `https://xin-ni.com/join?code=${code}`
  return await QRCode.toDataURL(url)
}
```

### 8.4 金额格式化（V1.1 新增）

```typescript
// 单价/金额统一保留两位小数展示（元）
const formatMoney = (value?: number | null) => {
  if (value === undefined || value === null || Number.isNaN(Number(value))) return '-'
  return Number(value).toFixed(2)
}
```

- 单价（`unit_price`）、金额（`amount`）展示均使用 `formatMoney` 保留两位小数
- 数量（`quantity`）按整数展示；维修时长（`repair_duration`）展示为"45 分钟"

---

## 第九章 开发顺序

| 顺序 | 模块 | 工作量 | 说明 |
| --- | --- | --- | --- |
| 1 | 项目初始化 + 登录页 | 小 | 打通登录流程 |
| 2 | API 封装 | 小 | 所有接口统一封装（含 V1.1 新增对账/导出接口） |
| 3 | 工单列表页 | 中 | Tab 切换 + 表格（含企业列）+ 筛选 |
| 4 | 工单处理页 | 中 | 状态操作 + 完工（含对账字段）+ 收据上传 |
| 5 | 修改对账信息（V1.1） | 小 | 已完成工单对账信息编辑（6.5） |
| 6 | 导出工单（V1.1） | 中 | 导出弹窗 + Excel 文件流下载（6.6） |
| 7 | 企业列表页 | 中 | 表格 + 搜索 |
| 8 | 企业详情页 | 中 | 成员管理 + 邀请码 + 待审核 |
