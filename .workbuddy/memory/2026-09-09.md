# 2026-09-09 工作日志

## 我的工单详情页"退回原因"显示优化
- 问题：详情页 `pages/order/detail.vue` 用裸的 `v-if="order.reject_reason"` 显示退回原因，但后端 `Submit` 重提时不清空 `reject_reason`，导致"已退回→重提→走完正常流程"的工单仍一直显示退回原因。
- 修复：将条件改为 `v-if="order.status === 'rejected' && order.reject_reason"`，仅当前状态确实为"已退回"时才展示（与"我的工单"列表页既有的 `status==='rejected'` 判断保持一致）。
- 备选方案（未实施）：若希望数据语义更干净，可在后端 `OrderService.Submit` 重提时清空 `RejectReason`；退回历史仍保留在时间轴 remark 中可追溯。

## 我的工单"进行中"漏掉待接单
- 现象："我的工单"→"进行中"拉不到 `pending_accept`（待接单）工单。
- 根因：`pages/order/list.vue` 第 127 行"进行中"Tab 的 status 串被改成了 `'reported,processing'`，丢掉了 `pending_accept`（被注释掉的原第 126 行才是含 pending_accept 的完整串）。
- 修复：改回 `'reported,pending_accept,processing'`。后端 `OrderService.List` 已支持该状态（statusLabels 含 pending_accept），无需后端改动。

## 状态显示胶囊改纯文字+颜色
- 用户反馈状态胶囊形"难看"，要求所有状态显示改为纯文字+颜色。
- 在 `utils/format.ts` 新增统一颜色映射 `TAG_TYPE_COLOR`（default灰/primary蓝/success绿/warning橙/danger红）及 `statusColor`/`memberStatusColor`/`enterpriseStatusColor`/`urgencyColor`。
- 改动点（`<wd-tag ... round>` → `<text :style="{color}">`）：
  - `components/order-card/order-card.vue`（驱动 我的工单/审核/处理 三列表的"当前状态"）
  - `pages/order/detail.vue`（我的工单详情）
  - `pages/admin/order/detail.vue`（后台工单详情）
  - `pages/admin/enterprise/list.vue` + `pages/admin/enterprise/detail.vue`（企业状态、成员状态）
- 校验：`tsc --noEmit` 通过（项目无 vue-tsc，仅校验 .ts；.vue 引用经 grep 确认）。

## 问题汇总文档整理成列表式
- 将甲方原始混乱文档 `docs/问题汇总-2026.8.29.md`（字段挤一行、序号错乱、错字）整理为列表式新文件 `docs/问题汇总-2026.8.29-整理版.md`，供逐条向甲方说明。
- 修正：原第 7 条序号误写为「1」重排 1-12；「项目大类关链」→「关联」；按手机小程序(11 项)/网页后台(1 项)分组。仅整理格式，未判断完成情况。

## 新建/编辑工单页返回键拦截
- 需求：点返回键退出时弹窗"是否保存该工单草稿？"——选"保存"=走保存逻辑并退出；选"不保存"=走删除逻辑并退出。
- 实现（`pages/order/edit.vue`）：
  - 新增页面级生命周期 `onBackPress()`：草稿已加载完成且非退出流程中时 `return true` 拦截系统/导航栏返回，弹窗后再退出；否则放行默认返回。
  - `saveDraft(exitAfter?)` / `deleteDraft(fromBack=false)` 改造为可复用：按钮调用 `saveDraft()`（保存不退出）、`deleteDraft()`（二次确认后删+退出）；返回场景下传 `true` 触发保存/删除后 `doExit()`。
  - 删除的二次确认合并进返回弹窗（返回场景 `fromBack=true` 不再重复弹"确定删除"）。
  - 用 `exiting` 标志位 + 遮罩关闭（confirm/cancel 均 false）保持停留，避免死循环与误删。
  - 底部按钮调用改为 `@click="saveDraft()"` / `@click="deleteDraft()"`，避免事件对象被误判为 `true`。
- 校验：`@vue/compiler-sfc` 解析+编译 edit.vue 的 script/template 均 OK。

## 编辑页返回拦截：脏检查 + 微信左上角"<"拦截
- 关键发现：微信小程序 `onBackPress` 只能监听、无法阻止返回（H5/App 才行）。微信端拦截左上角"<"必须用 `<page-container>` 的 `@beforeleave`。
- 脏检查：`init()` 末尾 `captureSnapshot()` 拍下 `form`+图片URL快照；`isDirty()` 对比当前状态。未改动时返回直接退出、不弹窗。
- 微信端：`#ifdef MP-WEIXIN` 用 `<page-container :show="containerShow" :overlay="false" @beforeleave="onContainerLeave">` 包裹整页；`onContainerLeave` 先把 `containerShow=false` 消耗一次返回事件，再 `promptExit()`（未加载完/已在退出则直接 `doExit`）。
- 非微信端：`onBackPress` 中 `#ifdef MP-WEIXIN return false` 放行，其余平台 `return true` 拦截。
- `promptExit()`：未 dirty→直接退出；有改动→弹窗"是否保存该工单草稿？"(保存/不保存)；点遮罩→停留并 `containerShow=true` 恢复拦截。
- 防自拦截：提交/参数错误自动 `navigateBack` 前设 `exiting=true`，`onContainerLeave` 见 exiting 直接放行，避免保存/提交后又弹窗。
- 校验：`@vue/compiler-sfc` 编译通过，关键符号(onContainerLeave/promptExit/isDirty/captureSnapshot/onBackPress)齐全。

## 编辑页返回拦截 Bug 修复（空白页 + 空草稿不弹窗）
- 空白页根因：之前把**整页内容都包进 `<page-container>`**。微信 page-container 会把子内容渲染到独立覆盖层，`beforeleave` 内 `containerShow=false` 消耗返回事件时该层隐藏，主页面内容为空 → 返回过程中闪空白页。
- 修复：`page-container` 改为**空兄弟节点**（仅 `:show`/`:overlay=false`/`@beforeleave`），不再包裹内容；真实内容留在主页面，`show` 切换不再影响可见内容。
- 空草稿不弹窗（真因）：`init()` 原先先做「仅一个可选单位时自动选中企业」**再**拍快照，导致单企业账号下草稿一加载 `enterprise_id` 就被自动填值 → 快照非空 → `isSnapshotEmpty()`/`isDirty()` 均 false → 直接退出、不弹窗。
- 修复：将 `captureSnapshot()`+`loaded=true` 提前到自动选企业**之前**；自动选择算作用户改动，基础加载态（企业未选）才作为脏检查基准。现单/多企业账号下新建空草稿点返回均会弹窗。
- 校验：`@vue/compiler-sfc` 编译通过；page-container 不再包裹内容，且快照时序已修正。

## 编辑页返回行为变更：空草稿自动删除（不再弹窗）
- 用户最终决定：进来完全不改（空草稿）返回时**直接自动删除**，不弹窗。
- 改 `promptExit()` 逻辑（移除 `shouldPrompt()`）：
  1. `isSnapshotEmpty()` 为真（加载时即空白草稿）→ `await deleteDraft(true)` 直接删+退出，不弹窗；
  2. 否则 `!isDirty()`（已填内容但本次未改）→ `doExit()` 保留草稿直接退出；
  3. 否则（有改动）→ 弹窗"是否保存该工单草稿？"（保存/不保存）。
- 边界：已填好的旧草稿若未改动，仅直接保留退出（不删、不弹窗），避免误删用户数据；只有"空白草稿且未改"才自动删。
- 校验：`@vue/compiler-sfc` 解析+编译 edit.vue OK；`shouldPrompt` 已无残留引用。

### 二次修正：单企业账号新建空草稿仍弹窗（根因）
- 根因①：原 `init()` 中「自动选中企业」发生在 `captureSnapshot()` **之后**，导致快照里 `enterprise_id` 为空、而实际被自动填充 → `isDirty()` 误判为"有改动"。
- 根因②：`isSnapshotEmpty()` 把 `urgency` 纳入空判，后端对 `urgency` 有默认值（如 'normal'）→ 误判"非空草稿"。两者叠加，空草稿落入了"有改动→弹窗"分支。
- 修复：
  1. `init()` 把 `captureSnapshot()` 移到「自动选中企业」**之后**拍，使自动选择不计入用户改动；
  2. 删除 `isSnapshotEmpty()`，新增 `isFormBlank()`——仅看用户手填核心内容（description/room/contact_name/contact_phone/图片），忽略企业、紧急程度等默认值或自动选择；`init()` 末尾据其计算 `wasEmptyOnLoad`；
  3. `promptExit()` 改用 `!isDirty() && wasEmptyOnLoad` 判定自动删除，`!isDirty()` 单独判定"未改保留退出"。
- 校验：`@vue/compiler-sfc` 解析+编译 edit.vue OK；`isSnapshotEmpty`/`shouldPrompt` 已无残留引用。

### 三次修正：半填草稿（未改）返回被误删
- 现象：进入“填了一半”的草稿编辑页、不动直接返回 → 被直接删除。
- 根因：`isFormBlank()` 只看 description/room/contact_name/contact_phone/images 四个“核心”字段。用户若只填了企业/分类/属性/紧急程度（description 还没填），这些字段仍空 → `wasEmptyOnLoad=true`，叠加 `isDirty()=false` → 落到自动删分支。
- 修复：废弃前端 form 视角的 `isFormBlank()`，改为基于**后端原始 `detail`** 全部可填字段的 `isBlankDraft(d)`（enterprise_id/category_id/property_id/description/urgency/room/contact/images 全空才为空白）；且 `wasEmptyOnLoad` 计算移到 `applyDetail` 之后、自动选企业**之前**，彻底隔离前端自动选择干扰。
- 结果：仅“后端存储的全空草稿且未改”才自动删；任意字段填过（含分类/紧急程度等）的草稿未改返回均保留；有改动仍弹窗。
- 校验：`@vue/compiler-sfc` 解析+编译 edit.vue OK；`isFormBlank` 已无残留引用。

## 我的工单：移除草稿「取消」按钮 + 取消弹窗样式统一
- 问题①：草稿/已退回卡片操作区（list.vue `#actions` 的 `draft||rejected` 分支）误放了「取消」按钮。草稿为"未提交"状态，应使用「删除」而非「取消工单」。
- 问题②：`cancelOrder`（list.vue、detail.vue）用的是 `uni.showModal({editable:true})` 系统原生弹窗（老样式），与 `order-action-bar` 的退回弹窗（`wd-popup` + `wd-textarea`）样式不一致。
- 修复：
  1. list.vue：删除 `draft||rejected` 分支的「取消」按钮（保留 编辑/提交/删除）；`reported/pending_accept` 分支的「取消」保留。
  2. list.vue & detail.vue：`cancelOrder` 改为打开 `wd-popup` 自定义弹窗（含 `wd-textarea` 填原因 + 取消/确认按钮，按钮用圆角矩形实心白字 `actBtnStyle`），新增 `confirmCancel()` 校验非空后调 `POST /orders/{id}/cancel`；`data` 加 `showCancelPopup/cancelReason/cancelSubmitting`（detail 另加 `actBtnStyle`），`style` 加 `popup-body/popup-title/popup-label/popup-actions`（与 order-action-bar 一致）。
- 文档同步：小程序前端任务文档 V1.3 §我的工单 第193行、需求规格 V1.3 第59/253/264行，将草稿从「可取消」状态移除，并标注取消弹窗统一为 wd-popup。
- 校验：`@vue/compiler-sfc` 解析+编译 list.vue、detail.vue 均 OK；`uni.showModal(editable)` 取消弹窗已无残留。

## 单位管理员「查询本单位业务及汇总」v1 范围决策
- 背景：V1.2 曾决策"小程序端不做审核员汇总"（决策②，统计/导出放 Web）；但 V1.3 规格 §3.5.3 又把汇总写在"小程序端单位审核员模块"下，两者冲突，本次重新确认。
- 用户决策（2026-09-09）：
  1. **展示端**：放在**小程序**（审核端）；但"具体放在哪个页面/位置"仍待商榷（未定）。
  2. **范围**：做到 **L2 标准汇总** = 状态分布 + 项目大类分布 + 报修人 Top5 + 时间范围筛选（覆盖规格 §3.5.3 承诺的总单数/各状态数/报修人排行/大类分布）。
  3. **导出**：Excel 导出放**二期**（审核员不自行导出对账，沿用店方/超管现有导出）。
- 落地依据：工单主表字段（status/category_id/urgency/报修人/created_at/completed_at/amount）+ 操作日志表足够支撑以上聚合；后端 `order_export.go` 已有 groupBy 聚合逻辑可复用；L1 计数卡可纯前端对 `/admin/orders` 各状态请求拿 total 实现（后端零改动），L2 需新增轻量 `stats` 聚合接口（GROUP BY status/category_id/reporter）。
- 待办：确认汇总区在小程序内的具体位置（审核工作台首页置顶卡？独立统计 Tab/页面？嵌入企业详情？）后，再进入实现。
