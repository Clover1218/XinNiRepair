# 2026-09-10
- 【修复 stats 两缺陷】① 5.21 StatsRepairerSummary 指定单业务员分支误移除 users JOIN 而 SELECT/GROUP 仍引用 users.nickname → 42P01；改为 scope 始终 JOIN users（单业务员加 WHERE repairer_id=?，全部取 IS NOT NULL）。DB 直查修复后 SQL 通过。② Web 统计页"全部企业/全部业务员"用空串 option 值导致 Element Plus 当未选择显示 placeholder：改用哨兵值 __all__（新增常量 ALL），范围下拉默认/判定/聚焦与返回全部逻辑同步（loadEntCard scope、loadRepCard repairer_id、repMetricItems、focus/back、模板 v-if=ALL）。vue-tsc+vite 通过。注意：后端需重启才生效。
- 【小程序处理工单统计模块(方案A)】需求：处理工单页加统计，展示可接单/个人已接/已完工等；给了5个UI方案，甲方选 A 顶部汇总卡(可折叠) + 口径 累计+今日双行。落地：后端新增 5.22 GET /admin/orders/stats/repairer-overview(staff 仅店方/超管；repairer_id?空=自己、enterprise_id?；返回 pending_accept/my_accepted/my_processing/my_completed/today_accepted/today_completed)；repository StatsRepairerOverview + service RepairerOverview + handler StatsRepairerOverview + staff 路由。同时加固 5.21：接单/完工显式 accepted_at/completed_at IS NOT NULL(保证无时间窗口累计口径正确)。小程序：types 加 RepairerOverview；pages/admin/order/list.vue 顶部统计卡(主行4累计 + 副行今日接单/今日完工/完工率；折叠态一行摘要；init/切换企业/动作done 刷新)。@vue/compiler-sfc 通过；DB 冒烟通过。文档：后端 V1.4补4；小程序任务文档 V1.3 新增 6.3 顶部统计卡。需重启后端生效。
- 【业务员归属过滤+越权防护闭环(后端, V1.4补5)】补齐用户原需求"业务员只能看/操作自己接的工单"。① 列表：GET /admin/orders 筛选含 processing 且为维修业务员(非超管)时，服务端强制 OrderAdminFilter.RepairerID=自身（repository ListForAdmin 据此加 repairer_id=?）。② 写操作：ensureOrderOwner(op,order) 已应用 complete/update_finance/reopen/receipts/reject(pending_accept|processing)；pending_accept 因 repairer_id 空不拦截（共享池）。③ 关闭同类越权查看：StatsRepairerSummary 与 StatsRepairerOverview 对维修业务员忽略传入 repairer_id 强制自身；Export mode=repairer 强制 repairer_id=自身。go build+go vet 通过。文档 V1.4补5。仍需重启后端 8080 生效。
- 【工单号格式变更 WO→XNB(V1.4补6)】用户要求工单号改为 XNB-{YYYYMMDD}-{3位当日流水号}（如 XNB-20260827-001）。落地：repository GenerateOrderNo 改前缀 `XNB-`+8位日期+`-`+3位序号（每日001重计）；model.OrderNo 注释、service/order.go 三处注释、单测 v14_flow_test 断言前缀 WO→XNB- 同步改。go build/vet 通过、单独验证逻辑正确（16字符<VARCHAR(20)）。文档：后端接口设计文档v1.2 的 §4.4 业务规则/示例 order_no/导出样表 + 修订记录 V1.4补6；数据库字段设计文档_V1.4 字段表/通知示例/附录A 代码 均已同步。历史 WO 工单因前缀不同不被新逻辑计数，互不冲突。仍需重启后端 8080 生效。
- 【前端文档示例刷 XNB】用户要求前端文档示例一并改 XNB。已更新 v2 现行前端文档：`新泥报修系统 - 小程序前端开发任务文档 V1.2.md`(行270/321/446: WO202608010001→XNB-20260801-001、WO202609060001→XNB-20260906-001)、`新泥报修系统-管理后台前端开发文档V1.2.md`(行542 同上)。v1 存档版(`docs/v1/*前端开发任务文档 V1.1 / 管理后台 V1.1`)仍含旧 WO 示例，属历史版本快照，未改动（如需可补）。
- 【工单管理搜索框改为仅工单号】用户要求：工单管理筛选框原 placeholder「工单号 / 项目 / 报修人」改为仅工单号搜索（项目、报修人已有独立下拉）。落地：① 前端 `web-frontend/src/views/orders/list.vue` 搜索框 placeholder →「工单号」，仍走 `params.keyword`。② 后端 `repository/order.go` `ListForAdmin` 的 keyword WHERE 由 `order_no OR category_name OR property_name OR description OR users.nickname` 收窄为仅 `repair_orders.order_no LIKE`。`Keyword` 仅被 ListForAdmin 消费；审核页(review) 不传 keyword，无副作用。go build/vet + vue-tsc 通过。③ 文档同步：后端接口文档 v1.2 行1279 keyword 说明改为仅 order_no，行1281 标注 order_no 独立参数未实现（统一走 keyword）；前端开发文档 V1.2 行450 同步。注：`JOIN users` 现仅作 1:1 FK（reporter_id 永非空），keyword 不再依赖，保留无害。仍需重启后端 8080。
